{
  "name": "universal-fs",
  "version": "2.0.0-canary-5",
  "description": "Allows a client or server to access the file system in the current codebase or a different one.",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "dev": "pnpx rollup -cw && pnpx nodemon && ../../dist/index.js",
    "build": "pnpx rollup -c && node ../../build/copy.mjs"
  },
  "devDependencies": {
    "rollup-plugin-copy": "^3.5.0",
    "rollup-plugin-typescript2": "^0.36.0"
  },
  "dependencies": {
    "@ngrok/ngrok": "^1.3.0",
    "bcrypt": "^5.1.1",
    "browser-or-node": "^3.0.0",
    "buffer": "^6.0.3",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
