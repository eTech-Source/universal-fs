{
  "name": "universal-fs",
  "version": "canary-1",
  "description": "Allows a client or server to access the file system in the current codebase or a different one.",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "dev": "pnpx rollup -cw && npx dts-bundle-generator ./index.ts --out-file dist/index.d.ts --no-check && pnpx nodemon dist/index.js",
    "build": "pnpx rollup -c && npx dts-bundle-generator ./index.ts --out-file dist/index.d.ts --no-check",
    "postinstall": "pnpm build &&node scripts/postinstall.js"
  },
  "devDependencies": {
    "dts-bundle-generator": "^9.5.1",
    "rollup-plugin-copy": "^3.5.0",
    "rollup-plugin-typescript2": "^0.36.0"
  },
  "dependencies": {
    "@ngrok/ngrok": "^1.3.0",
    "bcrypt": "^5.1.1",
    "browser-or-node": "^3.0.0",
    "buffer": "^6.0.3",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
